services:
  spelling:
    image: ghcr.io/nmemmert/spelling:latest
    container_name: spelling-v2
    ports:
      - "3000:3000"
    volumes:
      - spelling-data:/app/data
    restart: unless-stopped

volumes:
  spelling-data:
